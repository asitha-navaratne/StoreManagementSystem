type PropTypes = {
  isWindowOpen: boolean;
  handleClose: () => void;
  handleDisagreeAction: () => void;
  handleAgreeAction: () => void;
  windowTitle: string;
  description: string;
};

export default PropTypes;
